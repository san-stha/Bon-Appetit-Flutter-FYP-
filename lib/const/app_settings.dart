import 'package:flutter/material.dart';

class AppSetting {
  // Color settings
  static const primaryColor = Color.fromRGBO(8, 0, 49, 1);
  static const secondaryColor = Color.fromRGBO(119, 104, 193, 100);
  static const primaryText = Colors.black;
  static const secondaryText = Colors.black26;
  static const borderColor = Color(0xffe5e5e5);
  static const appBarColor = Color.fromRGBO(8, 0, 49, 100);
  static const ancentText = Colors.white;
  static const ancentColor = Colors.white;
  static const couponColor = Color.fromRGBO(234, 203, 195, 100);

  // Logo Setting
  // static const logo = "images/logo.png";

  // Default Loading Image
  // static const loading = "images/image.png";
}
